# include<stduo.h>
// function declaration 
int is prime( int num);
int main(){
int nunber;
printf(" enter a number:");
scanf("%d",&nunber);
if( is prime(number))
   printf(" %d is a prime nunber\n",number);
else
   printf("%d is not a prime number \n", number);
   return 0;
}
//function definition 
int is prime(int num){
if(nun<=1)
   return 0;//0 and 1 are not prime
for( int i= 2; i<=num/2; i++){
if(nun%i==0)
return 0;//not prime
}
  return 1;// prime
}
